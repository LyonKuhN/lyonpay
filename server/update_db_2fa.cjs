const { Pool } = require('pg'); require('dotenv').config({ path: '../.env' }); const pool = new Pool({ connectionString: process.env.DATABASE_URL, ssl: false }); async function main() { try { await pool.query('ALTER TABLE auth.users ADD COLUMN IF NOT EXISTS two_factor_preference BOOLEAN DEFAULT NULL, ADD COLUMN IF NOT EXISTS two_factor_code VARCHAR(6), ADD COLUMN IF NOT EXISTS two_factor_expires_at TIMESTAMP WITH TIME ZONE;'); console.log('Sucesso'); } catch (e) { console.error(e); } finally { await pool.end(); } }; main();
